// Demo account credentials — the account is pre-created by the Supabase
// migration (supabase/migrations/00001_init.sql) with the email confirmed,
// so "Try demo" signs in instantly. If the migration didn't run, the auth
// page auto-creates the account on first use.
export const DEMO_EMAIL = "demo@rtsp.me";
export const DEMO_PASSWORD = "demo123456";
